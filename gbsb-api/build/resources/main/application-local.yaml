# Local Docker environment - uses Oracle Autonomous DB with Wallet
# Using direct connect descriptor instead of TNS alias
spring:
  datasource:
    driver-class-name: oracle.jdbc.OracleDriver
    url: jdbc:oracle:thin:@(description=(retry_count=20)(retry_delay=3)(address=(protocol=tcps)(port=1522)(host=adb.ap-chuncheon-1.oraclecloud.com))(connect_data=(service_name=ga6d5c93e43a8a2_limocdv5x2d069pg_high.adb.oraclecloud.com))(security=(ssl_server_dn_match=yes)))
    username: ${DB_USERNAME:admin}
    password: ${DB_PASSWORD:Lch63877812@}
    hikari:
      maximum-pool-size: 5
      connection-timeout: 60000

  jpa:
    show-sql: true

logging:
  level:
    com.gbsb: DEBUG
    org.hibernate.SQL: DEBUG
